# The browser suite intentionally exercises the in-memory demo client. Keeping
# this opt-in in the test-mode environment prevents an absent production API
# URL from ever selecting it accidentally.
VITE_API_MOCK=true
